Einarbeitung in Objekte ist im Gang
Bewegung über x,y Koordinaten


(function() {

/* Glider deklaration und Bewegungsattribute
*/
var glider = function (glider-config, optcon) {
      if (glider_instanz) {
      return glider_instanz; 
            }
      }; 

glider_instanz = this;

this.config = glider-config  || optcon;


this.glider 


this.glider-congfig = {
      speed : 2,
      height : x,
      height : y


}


glider = 0

glider.config = {




};

var FPS = 60;

    }
)
